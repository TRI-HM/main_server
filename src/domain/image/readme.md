Sơ đồ flow:
Client → Server (Express) → App (HTTP/WS) (microservice) → Server → Client

Cần xây dựng một microservice để chạy trên máy có folder hình ảnh.
Có chức năng:

- Nhận yêu cầu từ server sẽ tìm ảnh trong thư mục.
- Gửi ảnh cho server
